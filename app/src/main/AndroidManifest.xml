<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.udacitynanodegree.cristhian.capstoneproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.IronHideApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Transparent"
        tools:replace="android:icon">
        <activity
            android:name=".ui.activities.MainActivity"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/Theme.Opaque">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="OPEN_ACTIVITY_1" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.AccountActivity"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/Theme.Opaque" />
        <activity
            android:name=".ui.activities.SplashActivity"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/Theme.Opaque" />
        <activity
            android:name=".ui.activities.VehicleDetailActivity"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/Theme.Opaque" />
        <activity
            android:name=".ui.activities.GoogleSignInActivity"
            android:screenOrientation="sensorPortrait"
            android:theme="@style/Theme.Opaque" />

        <provider
            android:name=".persistence.database.IronHideProvider"
            android:authorities="com.udacitynanodegree.cristhian.capstoneproject"
            android:exported="false"
            android:grantUriPermissions="true" />

        <service android:name=".service.IronHideFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".service.FirebaseIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/red_primary" />

        <service android:name=".service.ShowAlertService" />

        <activity
            android:name=".ui.activities.OrderReleasedActivity"
            android:label="@string/title_activity_order_released"
            android:theme="@style/Theme.Transparent"/>
    </application>

</manifest>